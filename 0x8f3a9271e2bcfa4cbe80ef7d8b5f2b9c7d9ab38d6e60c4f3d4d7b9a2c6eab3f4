<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Transaction — Ethereum Mainnet</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
  <style>
    :root {
      --bg:#0b0d10; --panel:#0f141a; --panel-2:#12161b; --muted:#97a3b6; --txt:#e9eef6; --accent:#66e0c2; --danger:#ff6b6b; --ok:#36d28f; --chip:#0f1620;
      --border:#1b2230; --border-2:#1e2a39; --shadow:0 12px 32px rgba(0,0,0,.35); --ring:0 0 0 2px rgba(102,224,194,.25);
      --grad1:#0e141c; --grad2:#0b0d10; --grad3:#090b0f;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body {
      margin:0; color:var(--txt);
      font:14px/1.55 Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
      background:
        radial-gradient(1200px 600px at 15% -10%, var(--grad1) 0%, var(--grad2) 45%, var(--grad3) 100%),
        linear-gradient(180deg, rgba(102,224,194,.06), transparent 40%);
    }
    body::before {
      content:""; position:fixed; inset:0; pointer-events:none; opacity:.05;
      background-image:
        linear-gradient(0deg, #fff 1px, transparent 1px),
        linear-gradient(90deg, #fff 1px, transparent 1px);
      background-size: 60px 60px;
      mix-blend-mode:overlay;
    }

    .wrap{max-width:1000px; margin:32px auto; padding:0 16px}
    .card {
      position:relative; background:linear-gradient(180deg, var(--panel), var(--panel-2));
      border:1px solid var(--border); border-radius:18px; box-shadow:var(--shadow); overflow:hidden;
    }
    .head{display:flex; align-items:center; justify-content:space-between; gap:16px; padding:22px 24px; border-bottom:1px solid var(--border)}
    .brand{display:flex; align-items:center; gap:12px}
    .brand .logo{width:28px; height:28px; display:grid; place-items:center}
    .brand .logo svg{width:100%; height:100%}
    .brand .chain{font-weight:700; letter-spacing:.2px}

    .status-pill{display:inline-flex; align-items:center; gap:8px; padding:6px 10px; border-radius:999px; font-weight:600; background:#0e1a15; color:var(--ok); border:1px solid rgba(54,210,143,.25)}
    .status-pill svg{width:16px; height:16px}

    .summary{display:flex; flex-wrap:wrap; gap:10px 14px; padding:18px 24px; border-bottom:1px solid var(--border)}
    .summary-item{display:flex; align-items:center; gap:10px; padding:8px 12px; background:var(--chip); border:1px solid var(--border-2); border-radius:12px}
    .summary .label{color:var(--muted)}

    .grid{display:grid; grid-template-columns: 1.15fr .85fr; gap:24px; padding:24px}
    .panel{background:#0e141b; border:1px solid var(--border); border-radius:14px; padding:18px}
    .section-title{margin:0 0 12px; font-weight:700}

    .kv{display:grid; grid-template-columns:180px 1fr; gap:10px 12px}
    .kv label{color:var(--muted)}

    .mono{font-family:ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace}
    .hash, .addr{display:flex; align-items:center; gap:8px}
    .code{padding:6px 10px; border-radius:10px; background:#0f141a; border:1px solid var(--border-2)}

    .copy{cursor:pointer; border:1px solid var(--border); background:#121a22; border-radius:9px; padding:6px 10px; color:var(--txt)}
    .copy:hover{background:#0f141a; box-shadow:var(--ring)}

    .badge{display:inline-flex; align-items:center; gap:8px; padding:4px 10px; border:1px solid var(--border); border-radius:10px; background:#0f141a; color:var(--muted)}
    .badge svg{width:14px; height:14px}

    .muted{color:var(--muted)}
    .toast{position:fixed; left:50%; transform:translateX(-50%); bottom:24px; padding:10px 14px; border-radius:10px; background:#0f1f18; color:#c7ffe8; border:1px solid rgba(54,210,143,.35); opacity:0; pointer-events:none; transition:opacity .2s ease, transform .2s ease}
    .toast.show{opacity:1; transform:translateX(-50%) translateY(-3px)}

    @media (max-width: 920px){ .grid{grid-template-columns:1fr} .kv{grid-template-columns:150px 1fr} }
    @media (max-width: 560px){ .kv{grid-template-columns:120px 1fr} .brand .chain{font-size:16px} }
  </style>
</head>
<body>
  <main class="wrap">
    <article class="card" aria-labelledby="tx-title">
      <header class="head">
        <div class="brand" aria-label="Network">
          <span class="logo" aria-hidden="true">
            <svg viewBox="0 0 256 417" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Ethereum logo">
              <path fill="#cde7df" d="M127.9 0l-3.1 10.6v274.5l3.1 3 127.9-75.6z" opacity=".85"/>
              <path fill="#9bd9c8" d="M127.9 0L0 212.5l127.9 75.6V0z" opacity=".65"/>
              <path fill="#cde7df" d="M127.9 320.2l-1.7 2v92.6l1.7 4.9 128-180.2z" opacity=".85"/>
              <path fill="#9bd9c8" d="M127.9 419.7v-99.5L0 239.5z" opacity=".65"/>
            </svg>
          </span>
          <span class="chain" id="tx-title">Ethereum Mainnet — Transaction</span>
        </div>
        <span class="status-pill" title="Transaction succeeded">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>
          Success
        </span>
      </header>

      <section class="summary" aria-label="Quick summary">
        <div class="summary-item">
          <span class="label">Value</span>
          <strong>422&nbsp;Ethereum</strong>
        </div>
        <div class="summary-item">
          <span class="label">Fee</span>
          <span class="mono">0.001986&nbsp;ETH</span>
        </div>
        <div class="summary-item">
          <span class="label">Block</span>
          <span class="mono">34941869</span>
        </div>
        <div class="summary-item">
          <span class="label">Time</span>
          <span>Sep&nbsp;1, 2025 • 5:21&nbsp;pm</span>
        </div>
      </section>

      <section class="grid" aria-label="Transaction data">
        <div class="panel">
          <h3 class="section-title">Overview</h3>
          <div class="kv">
            <label>From</label>
            <div class="addr">
              <code class="code mono">0x9u51…714hh</code>
              <button class="copy" data-copy-src="prev">Copy</button>
            </div>
            <label>Interacted Contract</label>
            <div class="addr">
              <code class="code mono">0x3dc3…a1764</code>
              <button class="copy" data-copy-src="prev">Copy</button>
            </div>
            <label>Transaction ID</label>
            <div class="hash">
              <code class="code mono">0x5f8c…e8f7b</code>
              <button class="copy" data-copy-src="prev">Copy</button>
            </div>
            <label>Value</label>
            <div><span class="badge">
              <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" aria-hidden="true"><path d="M12 1v22M5 6h8a6 6 0 0 1 0 12H5" fill="none" stroke="currentColor" stroke-width="2"/></svg>
              422&nbsp;Ethereum
            </span></div>
            <label>Transaction Fee</label>
            <div><span class="badge">0.001986&nbsp;ETH</span></div>
            <label>Network</label>
            <div><span class="badge">Ethereum Mainnet</span></div>
          </div>
        </div>

        <div class="panel">
          <h3 class="section-title">Transaction Details</h3>
          <div class="kv">
            <label>Status</label>
            <div class="mono" style="color:var(--ok); font-weight:600">Success</div>
            <label>Timestamp</label>
            <div>Sep 1, 2025 at 5:21 pm</div>
            <label>Block Number</label>
            <div class="mono">34941869</div>
            <label>Block Hash</label>
            <div class="hash">
              <code class="code mono">0xa4bf…2fca2e</code>
              <button class="copy" data-copy-src="prev">Copy</button>
            </div>
            <label>Method Id</label>
            <div class="mono">0xa64dfa75</div>
            <label>Gas Price</label>
            <div class="mono">94710319345</div>
            <label>Gas Used</label>
            <div class="mono">21000</div>
            <label>Nonce</label>
            <div class="mono">276</div>
          </div>
        </div>
      </section>
    </article>
  </main>

  <div class="toast" role="status" aria-live="polite" aria-atomic="true">Copied to clipboard</div>

  <script>
    (function(){
      const toast = document.querySelector('.toast');
      let hideTimer;
      function showToast(msg){
        toast.textContent = msg || 'Copied to clipboard';
        toast.classList.add('show');
        clearTimeout(hideTimer);
        hideTimer = setTimeout(()=> toast.classList.remove('show'), 1400);
      }

      function getTextFrom(el){
        if(!el) return '';
        if(el.dataset && el.dataset.full) return el.dataset.full;
        return el.textContent.trim();
      }

      document.addEventListener('click', (e)=>{
        const btn = e.target.closest('.copy');
        if(!btn) return;
        let src = null;
        if(btn.dataset.copySrc === 'prev'){
          src = btn.previousElementSibling;
        } else if (btn.dataset.copySrc) {
          src = document.querySelector(btn.dataset.copySrc);
        }
        const text = getTextFrom(src);
        navigator.clipboard.writeText(text).then(()=> showToast('Copied')).catch(()=> showToast('Copy failed'));
      });
    })();
  </script>
</body>
</html>
